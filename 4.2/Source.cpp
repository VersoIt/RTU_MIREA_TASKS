#include <iostream>
#include <fstream>
#include <string>

int sign(int number)
{
	return number < 0 ? -1 : number == 0 ? 0 : 1;
}

// «Знак числа
int main(int argc, char* argv)
{
	setlocale(LC_ALL, "Russian");

	return EXIT_SUCCESS;
}